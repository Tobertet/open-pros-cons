import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'open-pros-cons',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
